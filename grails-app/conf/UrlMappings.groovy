class UrlMappings {
	static mappings = {
        "/login/$action?"(controller: "login")
        "/logout/$action?"(controller: "logout")
        "/sprint/$action?"(controller: "sprint")
        "/team/$action?"(controller: "team")
        "/user/$action?"(controller: "user")
        "/promo/$action?"(controller: "promo")
    }
}
